# Writing style

Join independent clauses with a semicolon, not an em dash. Reserve the em dash for what a semicolon cannot do: parentheticals, appositives, interruptions.

# Comments

A function's doc comment carries its contract only — behavior, params, returns, raises, invariants, side effects.

Implementation reasoning goes inline, beside the code it explains: why this algorithm, why this order, why this edge case.
